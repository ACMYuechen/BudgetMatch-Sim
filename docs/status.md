# 项目状态

[文档导航](README.md) · [Agent 指南](agent.md) · [权限与安全](access-control.md)

整理日期：2026-09-21。以下依据仓库实现和已保留的验收记录，不是本次对服务器、数据库或外部模型的实时探测。

## 一句话结论

本轮 Agent 工程化与前端主体开发已进入交付收尾；本地自动化和多项真实依赖联调已有证据。**不能据此宣布生产全部完成，也不只有人工测试剩余**：最新版本发布验收、RPC 权限缺口和生产安全加固仍需单独处理。

## 已具备的能力

| 范围 | 已交付 | 验证边界 |
| --- | --- | --- |
| 电商与前端 | 认证、商品/订单、秒杀、支付接入，以及用户端和管理端页面 | 前端模拟 API 回归不等于完整真实支付/部署联调 |
| Agent 基础 | 结构化约束、多轮持久化、幂等重放、预算组合、工具权限、离线评测 | 规则终态门禁通过不等于任务质量达到 100% |
| Agent 检索与执行 | 一致快照索引、向量/关键词/RRF、候选实时核验、需求计划与受控执行 | 结构化执行默认关闭；关键词是其默认检索方式 |
| Agent 流式与可靠性 | 真实 RPC/SSE、取消传播、终态校验、并发隔离、用量记录和故障回归 | 取消不保证供应商停止计费；未完成 Usage/账单仍记 unknown |
| 本地实测 | 独立存储重启/恢复、真实登录与保留库、Flash、Nginx、二进制回退、BGE-M3 RAG 小样本 | 单机、合成商品和有限请求；不能外推为生产灾备或真实业务质量 |
| CI/CD | 按变更选择检查、CI Gate、镜像构建与 GitOps 候选版本 | Argo CD 仍需人工确认并同步，不是无条件自动发布 |

Agent M1–M6 的设计、执行分段和失败记录见[开发归档](archive/agent-development.md)；当前使用说明不再逐次复述阶段日志。

## 环境范围

| 环境 | 数据与用途 | 不应混淆的地方 |
| --- | --- | --- |
| 本地日常开发 | 复用 Docker，PostgreSQL `127.0.0.1:5432/budgetmatch-sim`，Redis `127.0.0.1:6379` | `.env.example` 的 PostgreSQL 默认端口仍为 15432，须按实例核对 |
| 本地保留验收库 | 历史 Agent/RAG 演示记录，位于另行管理的实例 | 不用于清理性集成测试，也不是日常业务数据源 |
| 自动化集成测试 | 测试入口临时提供连接和自有隔离资源 | 不从 `.env` / `DATABASE_DSN` 自动取业务连接；无显式连接时部分测试跳过 |
| 已部署 VPS | K3s 使用外部 PostgreSQL / Redis | 最近一次切库沿用了旧业务镜像，不能视为最新 Agent 已上线 |

已删除本地 Docker 实例中弃用的 `budgetmatch_sim_test` 库和 `budgetmatch_test` 账号；不需要为了普通开发重新创建。备份与删除范围见[数据维护归档](archive/local-data-2026-09.md)。容器是否此刻运行，应现场做只读检查。

## 仍需处理

1. **先修 RPC 权限缺口，再扩大暴露范围。** Auth 用户管理 RPC、Mall 订单归属和订单状态 RPC 存在 P0 问题；账号禁用/撤销、敏感日志、秒杀用户绑定等也未全部闭环。详见[风险清单](access-control.md#待修复风险)。前端按钮和 Admin 网关不能替代 RPC 自身鉴权。
2. **完成目标环境的版本发布与回滚验收。** 核对镜像、配置、数据库迁移、代理超时与完整请求链路，再验证新旧数据兼容和回滚。当前生产未完成最新 Flash / Embedding 切换，PostgreSQL 无 pgvector；现有本地回退记录不能替代 K3s 发布验收。
3. **补齐生产安全与运行条件。** 当前业务入口使用 HTTP，外部数据库连接未启用 TLS；需处理证书、访问来源约束、最小权限与网络隔离。支付配置也尚未完成。见 [VPS 运维](deployment-vps.md)和[权限与安全](access-control.md)。
4. **进行真实业务质量、负载与费用验收。** 现有 RAG 仅是小规模合成 SKU 闭环；实际供应商饱和背压、部署延迟、账单和真实商品效果仍需单独评估。历史调用授权不自动延续到下一轮。
5. **独立人工复核与端到端体验验收。** 64 条规则评测的人工复核仍待完成；终态门禁与可满足任务成功率是两种指标，已有后者为 25/40。浏览器经真实模型、商城、支付和目标部署的完整链路也不能用模拟测试替代。

这些事项需要不同授权、环境和验收标准，不宜简单合并成“只差一步”。文档整理本身不触发发版、迁移、密钥轮换、真实模型调用或数据清理。

## 证据入口

- [Agent 历史设计与验收索引](archive/README.md#agent-记录)
- [前端交付记录](archive/frontend-stages.md)
- [本地 Docker 恢复与测试库删除记录](archive/local-data-2026-09.md)
- [VPS 切库与分阶段验收记录](archive/deployment-vps-2026-09.md)
- [离线评测原始报告](../services/rpc/agent/testdata/eval/)
